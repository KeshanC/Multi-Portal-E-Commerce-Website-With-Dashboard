# E-Commerce-ETL
## Project Description
A premium, interactive single-page e-commerce dashboard and marketplace application. It features a developer-friendly sandbox to dynamically switch between three user roles: **Admin**, **Supplier**, and **User (Buyer)**. The application handles catalog product adjustments, shopping cart management, order dispatches, and custom SVG analytical data visualization, storing all state locally in the browser's `localStorage` to ensure persistence across page reloads.
---
## Technologies Used
- **Frontend Core**: HTML5, Vanilla CSS3 (Glassmorphism layout grids), Vanilla JavaScript (ES6+).
- **Data Visualization**: Custom SVG charts (Line charts, Progress gauges, Demographics splits) built without external libraries.
- **Session Control**: Active session trackers for sandbox role switching.
- **Storage**: Browser-based Local Storage acting as the database provider.
---
## Installation & Running Steps
### 1. Open Directory
Open your terminal (PowerShell or Command Prompt) and navigate to the project directory:
```bash
cd "/path/to/multi-role-shopping-portal"
2. Install Configurations
Initialize the node server dependencies:

bash


npm install
3. Run the Server
Launch the static host server locally:

bash


npm start
The local development server will start on port 8000.

4. Access the Website
Open your browser and navigate to: http://localhost:8000
